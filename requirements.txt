# Install festo-cpx-io from the local sibling repo (editable – picks up updates automatically).
# Switch to a pinned PyPI release once the library is published:
#   festo-cpx-io==<version>
-e ../festo-cpx-io

# FastAPI stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
